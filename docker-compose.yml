version: "3.8"

services:
  server-info-discordbot:
    build: .
    container_name: server-info-discordbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount surf map data (update path to your surf_.json location)
      - ./surf_.json:/usr/src/app/surf_.json:ro
